import * as React from 'react';
import { DraftBar } from 'vertice-design-system';

export const Default = () => (
  <DraftBar tag="RASCUNHO">
    Versão para revisão — as respostas enviadas aqui não entram na pesquisa oficial.
  </DraftBar>
);
